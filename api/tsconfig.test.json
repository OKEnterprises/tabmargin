{
  // Type-checks the test files, which the main tsconfig.json excludes.
  // Run via `npm run typecheck:test`.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["@cloudflare/workers-types", "node"]
  },
  "include": ["src/**/*.test.ts"],
  "exclude": []
}
